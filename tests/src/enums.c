enum {};
enum Foo {};
enum Bar {
    HELLO_WORLD = 6.5,
};
enum FooBar {
    HELLO = 50000000000000,
    WORLD = 1,
    FOOBAR,
};

enum Baz {
    X,
    Y,
    Z,
};

int X = Y + Z;

void foo0(enum Baz k) {
    enum Bark {
        A,
        B,
        C,
    };
}

enum Bark k = A;
